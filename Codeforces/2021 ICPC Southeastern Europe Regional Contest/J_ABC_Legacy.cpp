#include<bits/stdc++.h>
using namespace std;

#define N 200001

char input[N];

int main() {
	ios::sync_with_stdio(false);
	cin.tie(nullptr);
	int n;
	cin >> n;
	for (int i = 1; i < 2 * n; i++) {
		cin >> input[i];
	}

	return 0;
}